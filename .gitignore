# OS
.DS_Store
Thumbs.db

# Local scratch (design mockups, throwaway HTML, smoke fixtures)
_tmp/
_testResults/

# Local copies served by the python fixture server during in-browser
# verification (not the source-of-truth docs).
tests/fixtures/playwright-target/manual-test-plan.md
tests/fixtures/playwright-target/test-plan.html

# Playwright integration-test artifacts
test-results/
playwright-report/
**/tests/integration/.tmp/
**/tests/integration/.tmp-*/
tests/fixtures/recordings/**/.tmp/

# Editors
.vscode/
.idea/
*.swp

# Claude Code per-machine settings (commit .claude/settings.json if shared, never settings.local.json)
.claude/settings.local.json

# Secrets
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo

# Build artifacts
dist/
build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Terraform
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.backup
infra/terraform/*.tfvars
!infra/terraform/example.tfvars
infra/terraform/.terraform.lock.hcl

# Claude Code agent worktrees (transient)
.claude/worktrees/
