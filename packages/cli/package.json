{
  "name": "@webspec/cli",
  "private": true,
  "version": "0.0.0",
  "description": "webspec CLI — generate Jest specs, run WCAG/508 audits, and render Playwright specs from recordings.",
  "type": "module",
  "bin": {
    "webspec": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@webspec/config": "workspace:*",
    "@webspec/core": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
