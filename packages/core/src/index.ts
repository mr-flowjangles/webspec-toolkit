// @bellese/test-core — shared analyzer + renderer + LLM provider seam.
// Real exports land in M1 (Analysis contract + LLMProvider) and M2
// (TestPlanAnalyzer + TestRenderer). This stub keeps tsc -b satisfied at M0.
export const PACKAGE_NAME = '@bellese/test-core';
