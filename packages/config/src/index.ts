// @bellese/test-config — config schema + Angular project auto-detection.
// Real shape lands in M3 (CLI surface). This stub keeps tsc -b satisfied at M0.
export const PACKAGE_NAME = '@bellese/test-config';
