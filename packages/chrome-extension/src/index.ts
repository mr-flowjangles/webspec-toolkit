// @bellese/test-chrome-extension — Manifest V3 surface. Real popup + content
// script land in M5 (front-loaded as the flagship "easy to use" surface).
// tsconfig excludes Node types and adds DOM lib so this package is forced
// into a browser-shaped build from M0 onward.
export const CHROME_EXTENSION_STUB = true;
