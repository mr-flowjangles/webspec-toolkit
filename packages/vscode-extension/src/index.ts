// @bellese/test-vscode-extension — VS Code surface. Real activation lands in M5.
// In M5 this entry point becomes activate(context) per the VS Code extension API.
export const VSCODE_EXTENSION_STUB = true;
