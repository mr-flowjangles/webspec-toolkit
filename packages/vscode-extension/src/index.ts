// @bellese/test-vscode-extension — VS Code surface. Real activation lands in M6.
// In M6 this entry point becomes activate(context) per the VS Code extension API.
export const VSCODE_EXTENSION_STUB = true;
